import { IsDarkThemeDirective } from './is-dark-theme.directive';

describe('IsDarkThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDarkThemeDirective();
    expect(directive).toBeTruthy();
  });
});
